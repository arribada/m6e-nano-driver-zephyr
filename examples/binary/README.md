# Binary Sensor

This example demonstrates using the M6E Nano module to read RFID tags and display the tag EPCs on the console, removing them after X time when they are no longer in the range of the scanner.